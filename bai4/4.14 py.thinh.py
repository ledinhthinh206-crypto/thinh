# Giả định list ds ban đầu (chưa sắp xếp)
ds = ['cherry', 'banana', 'apple', 'date']

# 1. Sắp xếp các phần tử trong list (theo thứ tự tăng dần/alphabet)
ds.sort()

# 2. Lặp qua list đã sắp xếp và in từng phần tử
for ch in ds:
    print(ch)
